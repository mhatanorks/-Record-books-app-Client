
export const metadata = {
    title: "MBR-Search-page",
    description: "検索するんだ",
  };

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
      <div className="">{children}</div>
  );
}
